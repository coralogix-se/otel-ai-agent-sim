[pytest]
testpaths = tests
pythonpath = .
markers =
    live: queries a live Coralogix tenant via cx CLI
    catalog: validates local catalog YAML only
filterwarnings =
    ignore::DeprecationWarning
